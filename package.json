{
  "name": "@pixel-dust/root",
  "private": true,
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.2.5",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.56.2",
    "typescript": "^4.3.5"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "clean": "lerna exec -- yarn clean",
    "start": "yarn clean && lerna exec -- yarn start"
  }
}
